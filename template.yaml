apiVersion: scaffolder.backstage.io/v1beta3
kind: Template
metadata:
  name: vanilla
  title: Vanilla🍦
  description: Use this template if you need to create a new service with just bare bone minimum.
  tags:
    - vanilla
    - skeleton
    - api-docs
    - devex
spec:
  owner: Dev_IT_Developer_Experience@expedia.com
  type: service
  parameters:
    - title: Some fields
      properties:
        element:
          title: Test
          type: string
          description: Test
  steps:
    - id: placeholder
      name: Test Log
      action: debug:log
      input:
        message: |
          Hello World!
